import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// JSX => almost html jesa hee dikhta hai so ham isko html smj skte hai. iski help sai ham hamare custom tags bana paate hai. just like : '<App />' uppr dekho
